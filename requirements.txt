# Streamlit
streamlit>=1.32

# Core Data
pandas
numpy
tqdm
scikit-learn
Pillow>=10.0
joblib

# OpenCV — dùng contrib-headless để tránh conflict giữa easyocr và paddleocr
opencv-contrib-python-headless==4.10.0.84

# PaddleOCR
paddleocr
paddlepaddle>=3.2.2
pyclipper
shapely

# PyTorch CPU — trên Streamlit Cloud dùng bản CPU mặc định
torch>=2.0
torchvision>=0.15

# VietOCR Dependencies
einops==0.2.0
gdown==4.4.0
prefetch-generator==1.0.1
imgaug==0.4.0
albumentations==1.4.2
lmdb>=1.0.0
setuptools<70.0.0

# vietocr cài --no-deps (xem .streamlit/setup.sh bên dưới)